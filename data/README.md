This directory is intentionally empty in the GitHub-ready package.

Do NOT commit real runtime data here (state, ledgers, logs, control snapshots).
Initialize with:
  node scripts/run_task.mjs seed_state
